# RC1 体验反馈目录

每个问题使用独立 Markdown 文件，文件名格式：`RC1-001.md`。

创建步骤：

1. 复制 `docs/rc1-feedback-template.md`。
2. 填写可复现步骤、预期、实际、设备和脱敏证据编号。
3. 使用 `docs/rc1-defect-severity.md` 完成初步分级和归因。
4. 运行 `node scripts/rc1-feedback-summary.js` 生成汇总。

不要在本目录保存客户图片、真实账号、OPENID、Token、完整 URL、云凭证或完整业务记录。
