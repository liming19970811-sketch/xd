const assert = require('assert')
const fs = require('fs')
const path = require('path')

const root = path.resolve(__dirname, '..')
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8')
const runtime = read('utils/runtime/featureRuntimePolicy.js')
const execution = read('utils/task/generationExecution.js')
const taskLayer = read('utils/task/taskLayer.js')
const provider = read('cloudfunctions/generate_wanx/index.js')
const quota = read('cloudfunctions/quota_guard/index.js')

assert(runtime.includes("PRODUCTION: 'production'"))
assert(runtime.includes('mockFallbackEnabled === false'))
assert(execution.includes('createRealGenerationTask'))
assert(execution.includes('fallbackToMock: false'))
assert(taskLayer.includes('buildWanxResultMissingPatch'))
assert(provider.includes('verifyFormalProviderRequest'))
assert(provider.includes("resultMode: 'formal'"))
assert(provider.includes('formalProviderRequest'))
assert(provider.includes("action: 'rollbackUsage'"))
assert(provider.includes('{ image: wanxIdentityReferenceUrl }'))
assert(provider.includes('...wanxGarmentReferenceUrls.map((image) => ({ image }))'))
assert(quota.includes("process.env.ENABLE_REAL_QUOTA_GUARD || 'true'"))
assert(!provider.includes('real_provider_test'))
console.log('REAL_PROVIDER_FORMAL_CHANNEL_SMOKE_OK')
